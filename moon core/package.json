{
  "name": "moon-core",
  "version": "0.1.0",
  "description": "Orion's persistent soul — Cloudflare Worker + D1 backend",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create moon-core",
    "db:migrate:local": "wrangler d1 execute moon-core --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute moon-core --remote --file=./migrations/0001_init.sql",
    "db:console:remote": "wrangler d1 execute moon-core --remote --command='SELECT operator_id, created_at FROM operators'",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
